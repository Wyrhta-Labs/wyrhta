# --- Independent project repos (tracked in their own GitHub repos, not here) ---
/wyrhta-core/
/Heorth/
/heorth-mcp/
/KithLedger/
/Feoh/
/website/

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/*
!.vscode/extensions.json

# --- Node (in case any tooling lands at root) ---
node_modules/
npm-debug.log*

# --- Local Claude settings (personal overrides) ---
.claude/settings.local.json

# --- Execution tooling scratch state (snapshot lives in docs/execution-log.md) ---
.superpowers/

# --- Deployment secrets (deploy/.env.example is the committed template) ---
/deploy/.env*
!/deploy/.env.example

# --- Ad-hoc DB dumps taken as guardrails before destructive deploy changes ---
/deploy/*.sql

# --- Operator's private ops log ---
# Deployment state, not architecture: tenant identifiers, mailbox addresses,
# hostnames, credential record ids. Kept locally, never published. Removed from
# this repo's history 2026-08-19 (docs/going-public.md, item 1).
/docs/manual-todo.md
